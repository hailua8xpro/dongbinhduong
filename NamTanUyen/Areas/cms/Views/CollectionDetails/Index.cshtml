@model IEnumerable<NamTanUyen.Models.CollectionDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create",new { collectionId = ViewBag.CollectionId })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Collection.CollectionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.ProductName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Collection.CollectionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.ProductName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CollectionDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CollectionDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CollectionDetailId })
        </td>
    </tr>
}

</table>
